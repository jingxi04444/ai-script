# AI 智能体：爆款短视频脚本生成与复刻平台 - 后台（管理端）需求文档

## 文档信息



* **版本**：V1.0

* **状态**：脚本阶段 / MVP 至壁垒构建期

* **核心目标**：支撑前台用户全流程操作，实现数据管理、合规管控、权限隔离、系统配置，构建产品核心壁垒

* **使用人群**：系统超级管理员、品牌专属管理员、审核员、技术运维人员

## 一、核心原则与核心壁垒

### 核心原则



1. 多租户隔离：确保不同品牌客户的数据独立存储，互不可见，保障数据安全

2. 合规可控：通过违禁词库、审核流程、操作日志，满足合规审计要求

3. 高效支撑：优化 API 调用、数据存储、任务处理流程，保障前台用户使用体验

4. 可扩展性：预留接口与配置项，支持后续接入更多 AI 模型、平台渠道、功能模块

### 核心壁垒



* 私有知识库：沉淀各品牌的 “结构公式”“卖点库”“素材标签体系”，只存特征不存原文，兼顾复用性与合规性

* 全流程管控：从数据采集、脚本生成到素材管理、数据追踪，实现端到端的后台支撑

## 二、功能模块详细说明

### 模块 1：数据采集与解析管理（支撑前台步骤 3）

#### 功能描述

管理视频链接解析相关的 API、插件与日志，确保前台爆款复刻功能稳定运行。

#### 核心功能



1. 解析 API 配置：

* 支持配置多个第三方视频解析服务商（抖音、小红书）的 API Key

* 可设置 API 优先级（主备切换逻辑），当主 API 故障时自动切换至备用 API

* 配置 API 调用频率限制、超时时间、重试次数

* 支持测试 API 连通性，查看当前可用状态

1. 浏览器插件管理：

* 生成 / 管理 Chrome 浏览器插件安装包（供核心客户手动提取视频素材）

* 控制插件版本更新（发布新版本、强制更新、灰度发布）

* 管理插件授权名单（仅授权品牌客户可使用）

1. 解析日志监控：

* 记录每次前台用户的链接解析请求（请求时间、用户 ID、品牌 ID、链接地址、解析结果、耗时、使用 API）

* 支持按时间、品牌、解析状态（成功 / 失败）筛选日志

* 失败日志自动标记原因（如 API 故障、链接无效、权限不足），支持手动重试

1. 数据清洗规则：

* 配置解析数据的清洗逻辑（如去除冗余文本、提取核心文案、标准化数据格式）

* 定义结构化数据提取字段（确保点赞、评论等数据准确映射）

#### 操作权限

仅系统超级管理员、技术运维人员可操作

### 模块 2：知识库管理（核心壁垒模块）

#### 功能描述

管理品牌私有知识库与公共资源库，支撑 AI 生成脚本的精准性与复用性。

#### 2.1 品牌私有知识库（按品牌隔离）



| 子模块     | 功能描述                          | 操作功能                                                                                                                          |
| ------- | ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 结构公式库   | 存储从爆款分析中提取的 “结构公式”（仅存特征，不存原文） | 1. 自动同步前台步骤 3 的分析结果 > 2. 支持手动录入 / 编辑 / 删除结构公式按视频类型、投放平台、效果标签分类 4. 支持为公式添加适用场景说明（如 “适用于快消品促销”）5. 统计公式复用次数、生成脚本效果               |
| 产品卖点知识库 | 存储各品牌的标准化产品卖点信息               | 1. 自动同步前台步骤 2 的用户输入（经用户确认后）2. 支持批量导入 / 导出卖点数据（CSV 格式）>3. 支持按产品名称、品牌、更新时间检索 版本管理：保存卖点更新记录，支持回退5. 卖点标签管理（如 “核心卖点”“辅助卖点”“场景化卖点”） |
| 素材标签体系  | 管理各品牌的素材标签规则                  | 1. 预设标签类别（景别类型、情绪标签、产品展示方式等） 支持品牌自定义标签（需审核）3. 标签关联规则配置（如 “产品特写” 标签自动关联主卖点） 统计标签使用频率，优化标签体系                                    |

#### 2.2 公共资源库（全品牌共享）



| 子模块      | 功能描述                 | 操作功能                                                                              |
| -------- | -------------------- | --------------------------------------------------------------------------------- |
| 通用结构公式库  | 系统沉淀的通用爆款结构公式（跨品牌适用） | 1. 手动录入 / 编辑 / 分类标注公式适用平台、视频类型支持品牌按需导入到私有库                                        |
| 合规词库     | 内置合规相关词库，支撑前台合规检查    | 1. 《广告法》违禁词库（自动更新）2. 行业敏感词库（按行业分类配置）3. 替换建议库（为违禁词配置合规替换方案）4. 支持手动添加 / 删除 / 批量导入词库 |
| 场景 / 角色库 | 支撑前台视觉元素配置的公共资源      | 1. 预设通用场景（办公、厨房、户外等）、角色形象、道具库 > 2. 支持上传新场景图、角色素材，审核后上线 3. 分类管理（按场景类型、角色风格）        |

#### 操作权限



* 品牌专属管理员：仅可操作本品牌私有知识库

* 系统超级管理员：可操作所有知识库（公共资源库 + 各品牌私有库）

### 模块 3：生成与审核工作流管理（支撑前台步骤 4）

#### 功能描述

管理脚本审核流程、审核人员、审核记录，确保生成的脚本合规可用。

#### 核心功能



1. 审核席位管理：

* 为每个品牌分配审核员账号（支持多级审核：运营审核→法务审核→经理审核）

* 配置审核员权限（仅可审核本品牌脚本）

* 查看审核员工作负载（待审核数量、已审核数量、审核耗时）

1. 审核任务管理：

* 待审核列表：展示所有品牌待审核的脚本（按提交时间排序）

* 筛选功能：按品牌、脚本状态、提交人、审核阶段筛选

* 任务分配：系统自动分配或手动分配审核任务给审核员

* 催审功能：针对超时未审核的任务，可发送提醒给审核员

1. 审核规则配置：

* 配置合规检查触发条件（如违禁词高亮级别、原创度阈值）

* 配置审核流程（单级审核 / 多级审核，多级审核可设置审核顺序）

* 配置审核通过 / 驳回的通知方式（系统消息 / 邮件 / 短信）

1. 审核记录管理：

* 记录每个脚本的审核轨迹（提交时间、审核员、审核意见、修改记录、通过 / 驳回时间）

* 支持按脚本 ID、品牌、审核时间检索记录

* 记录不可删除，满足合规审计要求

#### 操作权限



* 审核员：仅可查看分配给自己的审核任务，执行审核操作

* 品牌专属管理员：可查看本品牌所有审核任务、审核记录，分配审核任务

* 系统超级管理员：可查看所有品牌审核任务、配置审核规则、管理审核员账号

### 模块 4：视频素材与项目库管理（支撑前台步骤 6-8）

#### 功能描述

管理所有品牌的项目数据与视频素材，确保素材安全、可追溯、可复用。

#### 核心功能



1. 项目管理：

* 每个前台脚本生成任务对应一个后台项目，记录项目状态（草稿 / 审核中 / 生成中 / 已完成 / 已归档）

* 展示项目核心信息（品牌 ID、创建人、创建时间、脚本名称、关联产品、素材数量）

* 支持按品牌、项目状态、创建时间筛选项目

* 支持项目归档、删除（仅超级管理员可删除，删除前需确认）

1. 素材管理：

* 按品牌隔离存储所有素材（视频片段、配音、场景图、参考素材）

* 素材详情页：展示素材关联项目、生成时间、标签、使用次数、存储路径

* 素材操作：支持预览、下载、批量导出、删除（品牌管理员可删除本品牌素材）

* 存储管理：查看各品牌素材存储占用情况，设置存储上限（超出上限提醒品牌清理或扩容）

1. 素材复用统计：

* 统计各素材的复用次数、复用项目、复用效果（关联前台投放数据）

* 生成素材质量报告（如 “高复用素材 TOP10”“低质量素材（需删除）TOP10”）

* 支持按素材类型、标签、复用率筛选统计结果

#### 操作权限



* 品牌专属管理员：仅可操作本品牌项目与素材

* 系统超级管理员：可操作所有项目与素材，查看存储全局统计

### 模块 5：投放数据看板（高阶功能，支撑前台步骤 9）

#### 功能描述

可视化展示各品牌脚本的投放效果数据，提供趋势分析与决策支持（MVP 版本暂不实现）。

#### 核心功能



1. 数据大屏：

* 全局数据：所有品牌脚本总播放量、总互动率（点赞 + 评论 + 收藏 + 分享）、总转化订单数、平均 ROI

* 品牌数据：按品牌展示核心数据（可切换单个品牌查看详情）

* 实时数据：展示近 24 小时 / 7 天数据趋势（折线图 / 柱状图）

1. 趋势分析：

* 行业爆款结构分析：基于第三方数据，展示本周 / 本月热门脚本结构 Top5

* 品牌效果趋势：展示单个品牌不同时期脚本的效果变化（播放量、转化率先）

* 卖点效果分析：统计不同卖点的转化效果，推荐高转化卖点

1. A/B 测试报告：

* 展示同一产品不同脚本的 A/B 测试结果（播放量、互动率、转化率对比）

* 自动标记胜出脚本，分析胜出原因（如结构公式、卖点植入方式）

* 支持导出 A/B 测试报告（PDF/Excel 格式）

#### 操作权限



* 品牌专属管理员：仅可查看本品牌数据看板与报告

* 系统超级管理员：可查看所有品牌数据，导出全局趋势分析报告

### 模块 6：系统与权限管理（基础支撑模块）

#### 功能描述

管理系统配置、用户账号、权限分配、操作日志，保障系统稳定安全运行。

#### 核心功能



1. 多租户隔离配置：

* 品牌租户管理：新增 / 编辑 / 禁用品牌租户（录入品牌名称、联系人、联系方式、开通时长）

* 数据隔离配置：确保各品牌数据（知识库、项目、素材、数据）独立存储，数据库层面隔离

* 品牌配置：为每个品牌配置专属域名、品牌 Logo、自定义皮肤（可选）

1. API 管理：

* 外部 API 配置：管理对接的 AI 模型 API（文生文、文生图、文生视频、TTS、数字人）的账号、Key、计费方式

* 大模型 Provider 配置需支持多个 OpenAI-compatible 供应商（如 DeepSeek、OpenAI、通义千问、豆包 / 火山方舟），配置存入 `api_provider_configs`，按启用状态、租户范围和 `priority` 自动选择；当主供应商调用失败时按优先级切换备用供应商。

* 大模型密钥可在数据库中保存加密值或 `env:VAR_NAME` 环境变量引用，接口返回时必须脱敏，不允许明文回显。

* 第三方平台 API 配置：管理抖音 / 小红书 / 视频号 / 飞瓜等平台的 API 授权，支撑链接解析、数据回传、一键发布

* API 调用监控：查看 API 调用次数、成功率、耗时，设置调用阈值（超出阈值提醒运维）

1. 用户与权限管理：

* 用户账号管理：新增 / 编辑 / 禁用用户账号（系统超级管理员、品牌专属管理员、审核员）

* 角色权限配置：预设角色（超级管理员、品牌管理员、审核员），支持自定义角色并分配权限（按模块细粒度权限控制）

* 登录管理：配置登录方式（账号密码登录 / 手机号验证码登录）、密码复杂度要求、登录超时时间

1. 操作日志与系统监控：

* 操作日志：记录所有用户的关键操作（账号登录、配置修改、审核操作、数据导出），包含操作人、操作时间、操作内容、IP 地址，不可删除

* 系统监控：监控服务器状态（CPU、内存、磁盘占用）、数据库状态、API 调用状态

* 告警配置：设置告警阈值（如服务器内存占用超 80%、API 调用失败率超 10%），配置告警方式（邮件 / 短信 / 企业微信）

#### 操作权限

仅系统超级管理员可操作

## 三、后台前端项目结构与联调规范

### 3.1 前端项目结构

`apps/admin-web` 需按生产前端结构拆分，不再把页面、路由、mock 和接口调用集中在单个 `App.tsx`。

```text
apps/admin-web/src/
├── app/
│   ├── App.tsx              # 后台应用入口、登录态、页面分发
│   ├── AdminLayout.tsx      # 后台布局、动态菜单、顶部用户信息
│   ├── router.tsx           # History API 路由工具
│   └── styles.css           # 后台全局样式
├── pages/
│   ├── AdminLoginPage.tsx
│   ├── AdminPage.tsx        # 模块页面分发
│   ├── DashboardPage.tsx
│   ├── ParsingPage.tsx
│   ├── KnowledgeBasePage.tsx
│   ├── AuditWorkflowPage.tsx
│   ├── MaterialsPage.tsx
│   ├── AnalyticsPage.tsx
│   ├── LLMProviderPage.tsx
│   ├── UserManagementPage.tsx
│   ├── RolePermissionPage.tsx
│   ├── OperationLogPage.tsx
│   └── SystemManagementPage.tsx
├── components/
│   ├── AdminActionModal.tsx
│   ├── DataTable.tsx
│   ├── MetricCard.tsx
│   ├── Panel.tsx
│   └── ToastView.tsx
├── services/
│   ├── apiClient.ts         # mock / 真实后端切换
│   ├── mock.js              # 本地 mock 数据与 mock API
│   ├── authApi.ts
│   ├── menuApi.ts
│   ├── dashboardApi.ts
│   ├── parsingApi.ts
│   ├── knowledgeApi.ts
│   ├── auditApi.ts
│   ├── materialApi.ts
│   ├── tenantApi.ts
│   ├── userApi.ts
│   ├── roleApi.ts
│   ├── operationLogApi.ts
│   ├── llmProviderApi.ts
│   └── analyticsApi.ts
└── types/
    ├── admin.ts             # 后台业务类型、接口参数、返回类型
    └── ui.ts                # Toast、弹窗等 UI 类型
```

### 3.2 API 调用与 mock / 真实后端切换

* 所有后台业务请求必须从 `src/services/*Api.ts` 发起，页面组件不得直接 import `mock.js`。

* 默认使用 `services/mock.js` 模拟接口，便于本地开发和无后端测试。

* 联调真实后端时通过环境变量切换：`VITE_USE_MOCK_API=false`；真实后端地址通过 `VITE_API_BASE_URL` 指定。

* `apiClient.ts` 负责统一 `request<T>()`、`callApi<T>()` 和 JSON 请求体序列化。

### 3.3 动态菜单栏需求

后台侧边栏菜单必须由后端接口动态返回，前端根据菜单配置和当前用户权限渲染，不允许在布局中写死菜单列表。

菜单配置字段：

```json
{
  "id": "dashboard",
  "label": "数据概览",
  "path": "/admin/dashboard",
  "permission": "dashboard",
  "enabled": true,
  "order": 10
}
```

渲染规则：

* `enabled=false` 的菜单不展示。

* 当前用户 `permissions` 不包含 `permission` 的菜单不展示。

* 菜单按 `order` 从小到大排序。

* 系统权限模块仅保留多租户管理和动态菜单配置；用户管理、角色权限、操作日志必须作为独立菜单页展示。

* 动态菜单配置支持编辑菜单名称、路径、权限标识、启用 / 停用、上移 / 下移。

* “系统权限” 菜单不得被停用，避免管理员无法恢复菜单配置。

### 3.4 后台 API 参数与返回报文

以下为当前生产前端已调用的 API 契约；mock 返回与真实后端应保持同构。

| 模块 | 方法 | 路径 | 请求参数 | 返回报文 |
| --- | --- | --- | --- | --- |
| 登录 | `POST` | `/api/admin/auth/login` | `{ "account": "admin@ai-script.local", "password": "123456" }` | `{ "token": "mock-admin-token", "user": { "id": "admin_001", "name": "系统管理员", "role": "超级管理员", "tenantScope": "全部品牌", "permissions": ["dashboard", "system"] } }` |
| 当前用户 | `GET` | `/api/admin/auth/current-user` | 无 | `{ "id": "admin_001", "name": "系统管理员", "role": "超级管理员", "tenantScope": "全部品牌", "permissions": ["dashboard", "parsing", "knowledge", "audit", "materials", "analytics", "llm", "users", "roles", "logs", "system"] }` |
| 菜单列表 | `GET` | `/api/admin/menus` | 无 | `[{ "id": "dashboard", "label": "数据概览", "path": "/admin/dashboard", "permission": "dashboard", "enabled": true, "order": 10 }]` |
| 更新菜单 | `PUT` | `/api/admin/menus` | `{ "menus": [{ "id": "dashboard", "label": "数据概览", "path": "/admin/dashboard", "permission": "dashboard", "enabled": true, "order": 10 }] }` | `{ "menus": [同请求菜单结构], "updatedAt": "刚刚" }` |
| 数据概览 | `GET` | `/api/admin/dashboard` | 无 | `{ "metrics": [{ "label": "今日项目", "value": "128", "delta": "+18%" }], "queues": [{ "name": "script-generation", "running": 18, "failed": 2, "successRate": "98.1%" }] }` |
| 解析日志 | `GET` | `/api/admin/parsing/logs` | 可扩展 `{ "brandId": "t1", "status": "failed", "page": 1, "pageSize": 20 }` | `[{ "id": "log_1", "brand": "北钥宠物生活", "platform": "抖音", "url": "douyin.com/video/7423", "status": "成功", "cost": "1.8s", "time": "14:20" }]` |
| 重试解析 | `POST` | `/api/admin/parsing/logs/{id}/retry` | 路径参数 `{ "id": "log_1" }` | `{ "id": "log_1", "status": "重试中" }` |
| 保存解析服务商 | `POST` | `/api/admin/parsing/providers` | `{ "provider": "主解析 API", "timeout": "8s", "retry": "2" }` | `{ "id": "provider_1710000000000", "status": "已保存", "payload": { ...请求参数 } }` |
| 大模型供应商列表 | `GET` | `/api/admin/llm/providers` | 无 | `[{ "id": "...", "providerName": "DeepSeek", "platform": "deepseek", "endpointUrl": "https://api.deepseek.com/v1", "model": "deepseek-chat", "priority": 10, "status": "enabled", "apiKeyRef": "env:DEEPSEEK_API_KEY" }]` |
| 新增大模型供应商 | `POST` | `/api/admin/llm/providers` | `{ "providerName": "DeepSeek", "endpointUrl": "https://api.deepseek.com/v1", "apiKeyRef": "env:DEEPSEEK_API_KEY", "model": "deepseek-chat", "priority": 10 }` | `{ "id": "...", "status": "created", "provider": { ...脱敏后的供应商配置 } }` |
| 停用大模型供应商 | `POST` | `/api/admin/llm/providers/{id}/disable` | 路径参数 `{ "id": "..." }` | `{ "id": "...", "status": "disabled" }` |
| 结构公式列表 | `GET` | `/api/admin/knowledge/formulas` | 可扩展 `{ "tenantId": "t1", "platform": "抖音" }` | `[{ "id": "f1", "name": "3 秒痛点 + 产品方案 + 轻 CTA", "platform": "抖音", "usage": 142, "risk": "低" }]` |
| 知识库导入 | `POST` | `/api/admin/knowledge/imports` | `{ "type": "合规词库", "fileName": "words.xlsx" }` | `{ "id": "import_1710000000000", "type": "合规词库", "fileName": "words.xlsx", "rows": 128, "status": "导入成功" }` |
| 审核任务列表 | `GET` | `/api/admin/audit/tasks` | 可扩展 `{ "brandId": "t1", "status": "pending", "assigneeId": "admin_002" }` | `[{ "id": "a_1", "script": "宠鲜鲜加热饭盒_v3", "brand": "北钥宠物生活", "owner": "林楠", "status": "待运营审核", "risk": "1 处词库风险", "submittedAt": "今天 14:20" }]` |
| 审核通过 | `POST` | `/api/admin/audit/tasks/{id}/approve` | 路径参数 `{ "id": "a_1" }` | `{ "id": "a_1", "status": "已通过", "message": "审核任务已通过并写入操作日志。" }` |
| 审核驳回 | `POST` | `/api/admin/audit/tasks/{id}/reject` | `{ "reason": "原创度过高" }` | `{ "id": "a_1", "status": "已驳回", "message": "审核任务已驳回，已通知提交人。" }` |
| 素材列表 | `GET` | `/api/admin/materials` | 可扩展 `{ "brandId": "t1", "type": "视频片段" }` | `[{ "id": "m1", "name": "办公室夜景参考", "type": "场景图", "brand": "北钥宠物生活", "project": "加热饭盒", "usage": 12, "size": "4.2MB" }]` |
| 删除素材 | `DELETE` | `/api/admin/materials/{id}` | 路径参数 `{ "id": "m1" }` | `{ "id": "m1", "status": "deleted" }` |
| 下载素材 | `POST` | `/api/admin/materials/{id}/download` | 路径参数 `{ "id": "m1" }` | `{ "id": "m1", "fileName": "素材包.zip", "url": "#" }` |
| 租户列表 | `GET` | `/api/admin/tenants` | 可扩展 `{ "status": "enabled" }` | `[{ "id": "t1", "name": "北钥宠物生活", "users": 18, "storage": "82GB", "status": "启用" }]` |
| 创建租户 | `POST` | `/api/admin/tenants` | `{ "name": "新品牌", "contact": "负责人" }` | `{ "id": "tenant_1710000000000", "status": "created", "payload": { ...请求参数 } }` |
| 用户列表 | `GET` | `/api/admin/users` | 可扩展 `{ "tenantId": "t1", "role": "审核员", "status": "enabled" }` | `[{ "id": "u1", "name": "许燃", "account": "xuran@ai-script.local", "role": "超级管理员", "tenantScope": "全部品牌", "status": "启用", "lastLogin": "今天 09:12" }]` |
| 创建用户 | `POST` | `/api/admin/users` | `{ "name": "新用户", "account": "new@ai-script.local", "role": "审核员", "tenantScope": "北钥宠物生活" }` | `{ "id": "admin_1710000000000", "status": "created", "payload": { ...请求参数 } }` |
| 停用用户 | `POST` | `/api/admin/users/{id}/disable` | 路径参数 `{ "id": "u1" }` | `{ "id": "u1", "status": "disabled" }` |
| 角色列表 | `GET` | `/api/admin/roles` | 无 | `[{ "id": "r1", "name": "超级管理员", "description": "可管理全部租户、菜单、权限和系统配置", "userCount": 2, "permissions": ["dashboard", "system"], "status": "启用" }]` |
| 创建角色 | `POST` | `/api/admin/roles` | `{ "name": "内容审核主管", "description": "管理审核员", "permissions": "audit,materials" }` | `{ "id": "role_1710000000000", "status": "created", "payload": { ...请求参数 } }` |
| 操作日志 | `GET` | `/api/admin/operation-logs` | 可扩展 `{ "operatorId": "admin_001", "module": "用户管理", "range": "7d" }` | `[{ "id": "op1", "operator": "系统管理员", "module": "动态菜单配置", "action": "更新菜单排序", "ip": "127.0.0.1", "time": "今天 14:32", "result": "成功" }]` |
| 投放概览 | `GET` | `/api/admin/analytics/summary` | 可扩展 `{ "brandId": "t1", "range": "7d" }` | `{ "plays": "326.8 万", "interactionRate": "8.7%", "orders": "2,418", "roi": "2.4" }` |

### 3.5 当前页面与服务映射

| 页面 | 文件 | 服务 |
| --- | --- | --- |
| 登录 | `pages/AdminLoginPage.tsx` | `services/authApi.ts` |
| 布局 / 动态菜单 | `app/AdminLayout.tsx` | `services/menuApi.ts` |
| 数据概览 | `pages/DashboardPage.tsx` | `services/dashboardApi.ts` |
| 解析管理 | `pages/ParsingPage.tsx` | `services/parsingApi.ts` |
| 知识库 | `pages/KnowledgeBasePage.tsx` | `services/knowledgeApi.ts` |
| 审核工作流 | `pages/AuditWorkflowPage.tsx` | `services/auditApi.ts` |
| 素材项目库 | `pages/MaterialsPage.tsx` | `services/materialApi.ts` |
| 投放数据 | `pages/AnalyticsPage.tsx` | `services/analyticsApi.ts` |
| 大模型配置 | `pages/LLMProviderPage.tsx` | `services/llmProviderApi.ts` |
| 用户管理 | `pages/UserManagementPage.tsx` | `services/userApi.ts` |
| 角色权限 | `pages/RolePermissionPage.tsx` | `services/roleApi.ts` |
| 操作日志 | `pages/OperationLogPage.tsx` | `services/operationLogApi.ts` |
| 系统权限 / 动态菜单 | `pages/SystemManagementPage.tsx` | `services/tenantApi.ts`、`services/menuApi.ts` |

## 四、MVP 版本最小功能集（优先实现）



1. 模块 1：数据采集与解析管理（基础 API 配置 + 解析日志）

2. 模块 2：知识库管理（品牌私有结构公式库手动录入 + 基础合规词库）

3. 模块 3：生成与审核工作流管理（单级审核 + 审核任务分配 + 审核记录）

4. 模块 6：系统与权限管理（多租户基础隔离 + 简单用户权限配置 + 操作日志）

> （注：文档部分内容可能由 AI 生成）
