<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiscript.modules.membership.mapper.AiBenefitUsageTransactionMapper">

    <select id="selectByRequestNoForUpdate"
            resultType="com.aiscript.modules.membership.entity.AiBenefitUsageTransaction">
        SELECT
            id, usage_id, user_id, benefit_code, request_no,
            amount, status, biz_type, biz_id, create_time, update_time
        FROM ai_benefit_usage_transaction
        WHERE request_no = #{requestNo}
        LIMIT 1
        FOR UPDATE
    </select>

</mapper>