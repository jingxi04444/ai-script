export type SourceAnalysis = {
  url: string;
  title: string;
  account: string;
  metrics: string;
  structure: string;
  report: string[];
};

export type OriginalTemplate = {
  id: string;
  name: string;
  structure: string;
};
